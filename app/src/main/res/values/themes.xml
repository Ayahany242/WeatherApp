<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Weather" parent="Theme.Material3.DayNight">
        <item name="fontFamily">@font/karma_regular</item>
        <item name="android:textColor">@color/black</item>
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="android:colorPrimary">@color/primaryColor</item>
        <item name="appBarLayoutStyle">@color/primaryColor</item>
        <item name="actionBarStyle">@style/Widget.AppCompat.ActionBar</item>
    </style>
    <style name="ActionBarTitle" parent="@style/TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textColor">@color/white</item>
    </style>
    <style name="Theme.Weather" parent="Base.Theme.Weather" />
</resources>